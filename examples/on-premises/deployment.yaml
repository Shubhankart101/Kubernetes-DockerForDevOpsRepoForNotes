apiVersion: apps/v1
kind: Deployment
metadata:
  name: devtrack-onprem
spec:
  replicas: 2
  selector:
    matchLabels: { app: devtrack-onprem }
  template:
    metadata:
      labels: { app: devtrack-onprem }
    spec:
      containers:
        - name: web
          image: registry.internal.example/devtrack:1.0.0
          ports: [{ containerPort: 80 }]
          readinessProbe:
            httpGet: { path: /, port: 80 }
---
apiVersion: v1
kind: Service
metadata:
  name: devtrack-onprem
spec:
  selector: { app: devtrack-onprem }
  ports: [{ port: 80, targetPort: 80 }]
  type: ClusterIP
