apiVersion: apps/v1
kind: Deployment
metadata:
  name: devtrack-azure
spec:
  replicas: 2
  selector:
    matchLabels: { app: devtrack-azure }
  template:
    metadata:
      labels: { app: devtrack-azure }
    spec:
      containers:
        - name: web
          image: YOUR_ACR.azurecr.io/devtrack:1.0.0
          ports: [{ containerPort: 80 }]
          readinessProbe:
            httpGet: { path: /, port: 80 }
---
apiVersion: v1
kind: Service
metadata:
  name: devtrack-azure
spec:
  selector: { app: devtrack-azure }
  ports: [{ port: 80, targetPort: 80 }]
  type: LoadBalancer
