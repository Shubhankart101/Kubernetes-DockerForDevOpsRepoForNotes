apiVersion: batch/v1
kind: Job
metadata: { name: database-migration }
spec:
  template:
    spec:
      restartPolicy: Never
      containers: [{ name: migrate, image: busybox:1.36, command: [sh, -c, 'echo migration'] }]
