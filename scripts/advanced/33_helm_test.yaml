apiVersion: v1
kind: Pod
metadata:
  name: "{{ include \"devops-app.fullname\" . }}-test"
  annotations: { helm.sh/hook: test }
spec:
  restartPolicy: Never
  containers:
    - name: test
      image: busybox:1.36
      command: [wget, "{{ include \"devops-app.fullname\" . }}:80"]
