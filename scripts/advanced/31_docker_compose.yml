services:
  api:
    build: .
    ports: ['8080:8080']
    depends_on: [db]
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_PASSWORD: replace-me
    volumes: [db-data:/var/lib/postgresql/data]
volumes:
  db-data:
